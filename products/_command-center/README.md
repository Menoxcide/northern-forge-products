# Northern Forge × Cognis Mesh Command Center

Unified real-time tracker aggregating:

- Cognis mesh nodes (TI / JUSTIN / MEG)
- gbrain + MEMORY.md / USER.md
- ADP products, revenue, marketing, stigmergy
- Watchdog, channels, security vault status

Open `index.html` or run `python3 command_center.py --port 8787` from the agent workspace.

Live regenerates via `mesh_context.py --refresh` and the orchestrator each cycle.
